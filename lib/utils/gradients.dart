// Color sets for gradient cards
import 'package:flutter/material.dart';

class GradientColors {
  // Teal gradient
  static const List<Color> teal = [
    Color(0xFF66F6F6), // Light teal
    Color(0xFF22A2A2), // Medium teal
    Color(0xFF008080), // Dark teal
    Color(0xFF66F6F6), // Repeat first color for smooth transition
  ];

  // Purple gradient
  static const List<Color> purple = [
    Color(0xFFE0B0FF), // Light purple
    Color(0xFF9932CC), // Medium purple
    Color(0xFF4B0082), // Deep purple
    Color(0xFFE0B0FF), // Repeat first color for smooth transition
  ];

  // Orange gradient
  static const List<Color> orange = [
    Color(0xFFFFB347), // Light orange
    Color(0xFFFF8C00), // Dark orange
    Color(0xFFCC5500), // Deep orange
    Color(0xFFFFB347), // Repeat first color for smooth transition
  ];

  // Blue gradient
  static const List<Color> blue = [
    Color(0xFF87CEEB), // Light blue
    Color(0xFF4682B4), // Steel blue
    Color(0xFF000080), // Navy blue
    Color(0xFF87CEEB), // Repeat first color for smooth transition
  ];

  // Green gradient
  static const List<Color> green = [
    Color(0xFF90EE90), // Light green
    Color(0xFF32CD32), // Lime green
    Color(0xFF006400), // Dark green
    Color(0xFF90EE90), // Repeat first color for smooth transition
  ];
}
